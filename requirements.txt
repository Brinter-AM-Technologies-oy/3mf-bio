# One place. Both workflows install from this file, so a workflow can no longer declare a
# dependency set that differs from what the code imports -- which is exactly how the second
# push failed: validate.yml installed lxml, verify.py builds the site, and the site build
# imports markdown.
#
# tools/ci_local.py checks this file against the repository's actual imports.

lxml            # XML Schema, ISO Schematron, both validators
markdown        # site/build_site.py

# Optional. tools/setup_repo.py validates CITATION.cff stays valid YAML if this is present,
# and says so plainly if it is not. Not needed by CI.
# pyyaml
